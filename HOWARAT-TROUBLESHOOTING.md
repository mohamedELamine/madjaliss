# استكشاف الأخطاء وإصلاحها - نظام الحوارات

## المشكلة: المشاركون لا يظهرون في صفحة الحوار

### خطوات التشخيص:

#### 1. تفعيل وضع Debug في WordPress

أضف هذه الأسطر في ملف `wp-config.php`:

```php
define( 'WP_DEBUG', true );
define( 'WP_DEBUG_LOG', true );
define( 'WP_DEBUG_DISPLAY', false );
```

#### 2. اختبار إضافة مشارك

1. افتح لوحة التحكم WordPress
2. انتقل إلى **حوارات** → **إضافة جديد**
3. أضف عنوان للحوار
4. في صندوق "المشاركون في الحوار":
   - اضغط على زر **"إضافة مشارك"**
   - اختر "ضيف" من القائمة المنسدلة
   - أدخل البيانات التالية:
     - الاسم: "د. أحمد محمود"
     - الدور: "ضيف"
     - السيرة الذاتية: "أستاذ الأدب العربي في جامعة القاهرة"
     - الرابط: https://example.com
   - اضغط على "رفع صورة" واختر صورة
5. **احفظ المنشور**

#### 3. التحقق من حفظ البيانات

##### أ) عبر واجهة WordPress:
1. بعد حفظ المنشور، أعد فتح صفحة التحرير
2. تحقق من أن المشارك الذي أضفته لا يزال موجوداً
3. إذا كان موجوداً = البيانات تُحفظ بشكل صحيح ✅

##### ب) عبر قاعدة البيانات:
افتح phpMyAdmin أو أي أداة لإدارة قواعد البيانات:

```sql
SELECT post_id, meta_value
FROM wp_postmeta
WHERE meta_key = 'dialogue_participants'
ORDER BY post_id DESC
LIMIT 5;
```

يجب أن ترى بيانات JSON مثل:
```json
[{"type":"guest","id":0,"name":"د. أحمد محمود","photo_id":123,"role":"ضيف","bio":"أستاذ الأدب العربي...","link":"https:\/\/example.com"}]
```

##### ج) عبر Debug Log:
افتح ملف `/wp-content/debug.log` وابحث عن:
```
Howarat Participants Saved for Post ID X: [JSON DATA]
```

#### 4. التحقق من عرض البيانات في الصفحة الأمامية

1. افتح صفحة الحوار في المتصفح
2. اضغط على **عرض مصدر الصفحة** (View Page Source)
3. ابحث عن:
```html
<!-- DEBUG - Participants JSON: -->
<!-- DEBUG - Participants Array: -->
```

إذا رأيت هذه التعليقات وكانت فارغة = المشكلة في الحفظ
إذا رأيت البيانات لكن لا تظهر في الصفحة = المشكلة في العرض

#### 5. التحقق من تحميل JavaScript

1. افتح صفحة تحرير الحوار
2. افتح **أدوات المطور** (F12)
3. انتقل إلى تبويب **Console**
4. ابحث عن أي أخطاء JavaScript باللون الأحمر
5. تحقق من أن ملف `howarat-meta.js` تم تحميله في تبويب **Network**

---

## الحلول الشائعة:

### المشكلة 1: زر "إضافة مشارك" لا يعمل
**السبب**: JavaScript لم يتم تحميله بشكل صحيح

**الحل**:
1. امسح الـ Cache من متصفحك
2. امسح الـ Cache من WordPress (إذا كنت تستخدم plugin للـ Cache)
3. تحقق من Console في أدوات المطور للبحث عن أخطاء

### المشكلة 2: البيانات لا تُحفظ
**السبب**: مشكلة في الصلاحيات أو nonce

**الحل**:
1. تحقق من أن لديك صلاحيات **edit_posts**
2. تحقق من ملف `debug.log` للبحث عن أخطاء PHP
3. تأكد من أنك تضغط "تحديث" أو "نشر" بعد إضافة المشاركين

### المشكلة 3: البيانات محفوظة لكن لا تظهر في الصفحة
**السبب**: مشكلة في التمبليت أو CSS

**الحل 1**: تحقق من أن القسم موجود في HTML
```javascript
// في أدوات المطور، شغّل هذا الكود في Console:
document.querySelector('.howarat-participants-section')
```

إذا كانت النتيجة `null` = القسم غير موجود في HTML (مشكلة في PHP)
إذا كانت النتيجة عنصر HTML = القسم موجود لكن مخفي (مشكلة في CSS)

**الحل 2**: تحقق من CSS
```css
/* ابحث في أدوات المطور عن العنصر .howarat-participants-section */
/* تأكد من أنه ليس display: none أو visibility: hidden */
```

### المشكلة 4: الصور لا تظهر
**السبب**: مشكلة في رفع الصور أو معرف الصورة

**الحل**:
1. تحقق من أن الصورة تم رفعها في مكتبة الوسائط WordPress
2. في قاعدة البيانات، تحقق من أن `photo_id` ليس صفراً
3. تحقق من صلاحيات مجلد `wp-content/uploads`

---

## بيانات اختبار JSON

يمكنك إضافة هذه البيانات مباشرة في قاعدة البيانات للاختبار:

```json
[
  {
    "type": "guest",
    "id": 0,
    "name": "د. أحمد محمود",
    "photo_id": 0,
    "role": "ضيف",
    "bio": "أستاذ الأدب العربي في جامعة القاهرة، له أكثر من 20 كتاباً في الأدب والنقد.",
    "link": "https://example.com/ahmad"
  },
  {
    "type": "guest",
    "id": 0,
    "name": "أ. فاطمة علي",
    "photo_id": 0,
    "role": "مقدمة",
    "bio": "صحفية وإعلامية، تقدم برنامج الحوار الثقافي منذ 5 سنوات.",
    "link": "https://example.com/fatima"
  }
]
```

**كيفية إضافتها**:
```sql
-- استبدل XXX برقم ID الحوار الخاص بك
UPDATE wp_postmeta
SET meta_value = '[{"type":"guest","id":0,"name":"د. أحمد محمود","photo_id":0,"role":"ضيف","bio":"أستاذ الأدب العربي","link":"https://example.com"}]'
WHERE post_id = XXX
AND meta_key = 'dialogue_participants';
```

---

## معلومات تقنية

### هيكل البيانات المتوقع:

كل مشارك يجب أن يحتوي على:
- `type` (string): "guest" أو "user"
- `id` (int): 0 للضيوف، user_id للمستخدمين المسجلين
- `name` (string): اسم المشارك (إلزامي)
- `photo_id` (int): معرف الصورة في مكتبة الوسائط
- `role` (string): دور المشارك (اختياري)
- `bio` (string): السيرة الذاتية (اختياري)
- `link` (string): رابط خارجي (اختياري)

### الملفات المسؤولة:

1. **Meta Box (الإدخال)**:
   - PHP: `nadiim-theme/inc/howarat-meta-box.php`
   - JS: `nadiim-theme/assets/js/howarat-meta.js`

2. **العرض الأمامي**:
   - Template: `nadiim-theme/single-howarat.php`
   - CSS: `nadiim-theme/assets/css/howarat.css`

3. **قاعدة البيانات**:
   - الجدول: `wp_postmeta`
   - meta_key: `dialogue_participants`
   - meta_value: JSON string

---

## طلب المساعدة

إذا لم تحل المشكلة بعد اتباع هذه الخطوات، يرجى توفير المعلومات التالية:

1. نسخة WordPress:
2. نسخة PHP:
3. هل هناك أخطاء في debug.log؟ (الصق الأخطاء هنا)
4. هل JavaScript يعمل؟ (نعم/لا)
5. هل البيانات محفوظة في قاعدة البيانات؟ (نعم/لا)
6. لقطة شاشة من Console في أدوات المطور
7. لقطة شاشة من صفحة تحرير الحوار
