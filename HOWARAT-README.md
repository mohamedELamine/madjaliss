# نظام الحوارات (Howarat) - دليل الاستخدام الكامل

## 📚 نظرة عامة

تم إنشاء نظام الحوارات الكامل لقالب نديم، يتضمن:
- Custom Post Type للحوارات (howarat)
- ميتا بوكس متقدم لإدارة المشاركين
- صفحة مفردة احترافية مع مشغل فيديو/صوت
- صفحة أرشيف بتصميم بطاقات Grid
- CSS كامل ومنظم
- دعم RTL كامل

---

## 🗂️ بنية الملفات

```
nadiim-theme/
├── inc/
│   ├── cpt-howarat.php             # تسجيل CPT الحوارات
│   └── howarat-meta-box.php        # ميتا بوكس المشاركين
├── assets/
│   ├── css/
│   │   └── howarat.css            # CSS كامل للحوارات
│   └── js/
│       └── howarat-meta.js        # JS لإدارة الميتا بوكس
├── single-howarat.php              # قالب الصفحة المفردة
├── archive-howarat.php             # قالب الأرشيف
└── functions.php                   # (محدث لتضمين الملفات الجديدة)
```

---

## 🚀 التثبيت والاستخدام

### 1. التفعيل

الملفات محملة تلقائياً عبر `functions.php`. بعد رفع الملفات:

1. افتح لوحة تحكم WordPress
2. اذهب إلى **الإعدادات ← روابط دائمة**
3. اضغط **حفظ التغييرات** (لتحديث rewrite rules)
4. ستظهر **الحوارات** في القائمة الجانبية بأيقونة 💬

### 2. إنشاء حوار جديد

1. اذهب إلى **الحوارات ← إضافة جديد**
2. أدخل:
   - **العنوان**: عنوان الحوار
   - **المحتوى**: النص الكامل للحوار
   - **المقتطف**: ملخص قصير
   - **الصورة البارزة**: صورة الغلاف (يُفضل 16:9)

3. في **صندوق الحقول المخصصة** (أسفل المحرر)، أدخل:
   - `dialogue_date`: تاريخ الحوار (مثل: 2026-03-15)
   - `dialogue_type`: نوع الحوار (مثل: مقابلة مسجلة، نقاش، بودكاست)
   - `dialogue_duration`: المدة (مثل: 45 دقيقة)
   - `dialogue_media`: رابط الفيديو أو الصوت (YouTube, SoundCloud, ملف MP3)
   - `dialogue_media_type`: `video` أو `audio` أو `podcast`

### 3. إضافة المشاركين

في صندوق **"المشاركون في الحوار"** (أعلى الصفحة):

1. اضغط **"إضافة مشارك"**
2. اختر **نوع المشارك**:
   - **مستخدم مسجل**: اختر من قائمة المستخدمين
   - **ضيف**: أدخل البيانات يدوياً

3. املأ الحقول:
   - **الاسم**: (للضيوف فقط)
   - **الدور**: مثل "ضيف"، "منسق"، "مقدم"
   - **السيرة الذاتية**: نبذة مختصرة
   - **الرابط**: رابط الموقع أو الملف الشخصي
   - **الصورة**: ارفع صورة شخصية (دائرية 64px)

4. يمكنك إضافة عدد غير محدود من المشاركين
5. لحذف مشارك، اضغط **"حذف"**

---

## 📝 مثال JSON للمشاركين (للمطورين)

عند حفظ الحوار، يتم تخزين المشاركين بصيغة JSON في `dialogue_participants`:

```json
[
  {
    "type": "guest",
    "id": 0,
    "name": "د. أحمد محمود",
    "photo_id": 123,
    "role": "ضيف",
    "bio": "أستاذ الأدب العربي بجامعة القاهرة، متخصص في الشعر الجاهلي",
    "link": "https://example.com/ahmad"
  },
  {
    "type": "user",
    "id": 5,
    "name": "فاطمة علي",
    "photo_id": 456,
    "role": "مقدمة",
    "bio": "كاتبة وصحفية متخصصة في الثقافة",
    "link": ""
  }
]
```

### شرح الحقول:

- **type**: `guest` (ضيف) أو `user` (مستخدم مسجل)
- **id**: 0 للضيوف، أو `user_id` للمستخدمين المسجلين
- **name**: اسم المشارك (يُجلب تلقائياً من المستخدم إذا كان النوع `user`)
- **photo_id**: معرف الصورة من Media Library
- **role**: دور المشارك (ضيف، منسق، مقدم، إلخ)
- **bio**: السيرة الذاتية (نص قصير)
- **link**: رابط خارجي (اختياري)

---

## 🎨 التخصيص والتصميم

### الألوان

يمكن تغيير الألوان في `assets/css/howarat.css`:

```css
:root {
	--howarat-primary: #339063;        /* اللون الأساسي */
	--howarat-primary-dark: #26704A;   /* اللون الأساسي الداكن */
	--howarat-accent: #F4A340;         /* اللون المميز */
	/* ... */
}
```

### حجم الهيرو

في `single-howarat.php`:

```css
.howarat-hero {
	min-height: 520px; /* غيّر هذا الرقم */
}
```

### عدد الأعمدة في الأرشيف

في `assets/css/howarat.css`:

```css
.howarat-grid {
	grid-template-columns: repeat(3, 1fr); /* غيّر 3 إلى العدد المطلوب */
}
```

---

## 🛠️ دوال PHP المساعدة

### جلب المشاركين

```php
$participants_json = get_post_meta( get_the_ID(), 'dialogue_participants', true );
$participants = json_decode( $participants_json, true );

if ( ! empty( $participants ) && is_array( $participants ) ) {
    foreach ( $participants as $participant ) {
        echo $participant['name'];
        echo $participant['role'];
        // ...
    }
}
```

### جلب بيانات الحوار

```php
$dialogue_date     = get_post_meta( get_the_ID(), 'dialogue_date', true );
$dialogue_type     = get_post_meta( get_the_ID(), 'dialogue_type', true );
$dialogue_duration = get_post_meta( get_the_ID(), 'dialogue_duration', true );
$dialogue_media    = get_post_meta( get_the_ID(), 'dialogue_media', true );
```

---

## 📱 الاستجابة (Responsive)

التصميم مستجيب بالكامل:

- **Desktop** (>1024px): 3 أعمدة + sidebar
- **Tablet** (768-1024px): عمودان
- **Mobile** (<768px): عمود واحد

---

## ♿ الوصولية

- جميع الأزرار والروابط لها `focus states`
- دعم `prefers-reduced-motion`
- دعم `prefers-contrast`
- تسميات ARIA مناسبة

---

## 🧪 الاختبار

### اختبار الميتا بوكس:

1. أنشئ حواراً جديداً
2. أضف مشاركاً (ضيف)
3. أضف مشاركاً (مستخدم مسجل)
4. احفظ
5. أعد فتح الحوار للتأكد من حفظ البيانات

### اختبار الصفحة المفردة:

1. أنشئ حواراً مع رابط YouTube
2. أضف مشاركين
3. احفظ وانتقل للصفحة المفردة
4. تأكد من:
   - ظهور الهيرو بشكل صحيح
   - تشغيل الفيديو
   - ظهور المشاركين بصورهم
   - Sidebar يعمل

### اختبار الأرشيف:

1. أنشئ 3-5 حوارات
2. اذهب إلى `/howarat`
3. تأكد من:
   - ظهور البطاقات في Grid
   - hover effects تعمل
   - pagination يعمل

---

## 🐛 حل المشاكل

### المشاركون لا يظهرون

تأكد من:
1. حفظ الحوار بعد إضافة المشاركين
2. الملف `howarat-meta-box.php` محمّل في `functions.php`
3. JavaScript محمّل (`howarat-meta.js`)

### الفيديو لا يعمل

تأكد من:
1. الرابط صحيح (YouTube, Vimeo, إلخ)
2. `dialogue_media_type` محدد (`video`)
3. WordPress oEmbed مفعّل

### CSS لا يظهر

تأكد من:
1. الملف `howarat.css` موجود في `/assets/css/`
2. `wp_enqueue_style` يُستدعى في القوالب
3. تنظيف الـ cache

---

## 📊 الإحصائيات

- **عدد الملفات**: 7 ملفات
- **عدد أسطر CSS**: ~900 سطر
- **عدد أسطر PHP**: ~1200 سطر
- **عدد أسطر JS**: ~100 سطر
- **دعم RTL**: ✅ كامل
- **دعم Gutenberg**: ✅ (`show_in_rest: true`)

---

## 📧 الدعم

للأسئلة والمشاكل، يرجى التواصل عبر:
- GitHub Issues
- البريد الإلكتروني
- المنتدى

---

## 📜 الترخيص

هذا النظام جزء من قالب نديم ومرخص تحت نفس الترخيص.

---

**تم بناء هذا النظام بـ ❤️ لقالب نديم**
